export const environment = {
  production: false,
  /** Empty uses dev-server proxy to backend (`proxy.conf.json`). */
  apiBaseUrl: 'https://airship-app-production.up.railway.app',
  /** Public Angular app origin for CMS iframe mirror (`Site content → home`). */
  publicSiteOrigin: '',
};
